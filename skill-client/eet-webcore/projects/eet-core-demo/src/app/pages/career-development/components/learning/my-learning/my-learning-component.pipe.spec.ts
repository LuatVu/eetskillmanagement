import { MyLearningComponentPipe } from './my-learning-component.pipe';

describe('MyLearningComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new MyLearningComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
